{
  "name": "vision-remota",
  "version": "1.1.0",
  "private": true,
  "description": "Sistema de vigilancia WebRTC con detección de movimiento y alarmas",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0",
    "test:motor": "node --test tests/motion.test.js",
    "test:app": "node --test tests/app.integration.test.js"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
